

<section class = "content">
  <h1>Products</h1>
<ul>
  <li><%= link_to("All Partners", {:controller => "products", :action => 'all_partners'}) %></li>
  <li><%= link_to("Top Design", {:controller => "products", :action => 'top_design'}) %></li>
  <li> <%= link_to("Our Products", {:controller => "products" , :action => 'our_rooms', :group_id => @group.id}) %></li>
</ul>


<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @products.each do |product| %>
  <tr>
    <td><%= product.name %></td>
    <td><%= link_to 'Show', product %></td>
    <td><%= link_to 'Edit', edit_product_path(product) %></td>
    <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product', new_product_path %>

</section>